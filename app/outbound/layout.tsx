export const metadata = {
  title: 'Outbound Management - Warehouse System',
  description: 'Manage warehouse stock-out operations and dispatch workflows',
};

export default function OutboundLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
